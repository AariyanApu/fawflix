import Feed from '@/component/Feed';
import MovieList from '@/component/MovieList';

const Home = () => {
  return (
    <section>
      <Feed />
      <MovieList />
    </section>
  );
};

export default Home;
